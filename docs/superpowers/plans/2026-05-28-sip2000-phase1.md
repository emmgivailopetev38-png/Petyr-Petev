# СИП 2000 Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace ZOPEXPERT's 6 generic chats with 6 specialised СИП 2000 workspaces. Each workspace injects a vertical-specific system prompt before the Hermes call (food / construction / supervision / deliveries / services / framework). The Hermes wrapper adds anti-freeze (50s timeout) and one auto-retry on empty/timeout so users never see a blank bubble.

**Architecture:** System prompts live as constants in `lib/prompts/sip2000.ts` (source of truth) and are seeded into the `chats` table via migration. The chat route reads `system_prompt` from the workspace row and prepends it as the first message. A new `lib/hermes-client.ts` wraps the OpenAI streaming call with timeout + retry semantics. UI shows workspace icon, title, welcome message, and a context-aware placeholder. Future RAG, playbooks, and auditor agents will compose with this same row-based workspace contract — no refactor needed.

**Tech Stack:** Next.js 16, React 19, TypeScript, Supabase (Postgres + pgvector later), Hermes OpenAI-compatible API, Vitest 4. No new deps.

---

## File Map

```
ZOPEXPERT/
├── supabase/migrations/
│   └── 003_workspaces.sql                  # CREATE: adds columns + seeds 6 workspaces
├── lib/
│   ├── types.ts                            # MODIFY: extend Chat with system_prompt etc.
│   ├── prompts/
│   │   └── sip2000.ts                      # CREATE: 6 system prompts as constants
│   └── hermes-client.ts                    # CREATE: timeout + retry wrapper around OpenAI client
├── app/
│   ├── page.tsx                            # MODIFY: select includes new columns implicitly via '*'
│   └── api/hermes/chat/
│       ├── route.ts                        # MODIFY: prepend system prompt + use hermes-client
│       └── route.test.ts                   # MODIFY: new tests for system prompt + retry
└── components/
    └── ChatPanel.tsx                       # MODIFY: render icon, welcome message, placeholder
```

---

## Task 1: DB migration + seed 6 workspaces

**Files:**
- Create: `ZOPEXPERT/supabase/migrations/003_workspaces.sql`

The migration adds 4 columns to `chats` and updates the existing 6 rows with СИП 2000 specialization. Defaults keep existing rows valid (no NOT NULL without default).

- [ ] **Step 1: Create `ZOPEXPERT/supabase/migrations/003_workspaces.sql`**

```sql
-- Workspace specialization columns
alter table chats add column if not exists system_prompt text;
alter table chats add column if not exists welcome_message text;
alter table chats add column if not exists icon text default '💬';
alter table chats add column if not exists vertical text;

-- Seed: 6 СИП 2000 workspaces.
-- Full system prompts live in lib/prompts/sip2000.ts; here we keep a concise version
-- so the DB is self-contained. Code will overwrite from constants on each deploy
-- via a separate sync (Phase 1.5). For now, the prompts here are authoritative.

update chats set
  title = 'Храни и хранителни продукти',
  vertical = 'food',
  icon = '🍎',
  welcome_message = 'Готов съм да работя по обществени поръчки за храни. Качи документация или ме питай за изисквания по БДС, HACCP, хладилна верига, аналози по чл. 49 ЗОП.',
  system_prompt = 'Ти си експерт-консултант по обществени поръчки за храни и хранителни продукти в България. Работиш съгласно ЗОП и ППЗОП.\n\nСпецифика на вертикала:\n- БДС стандарти за хранителни продукти (БДС EN 14108, БДС EN 13104 и др.)\n- Хладилна верига и температурни режими (+2 до +8°C за охладени, под -18°C за дълбоко замразени)\n- Срокове на годност в техническите спецификации\n- Сертификати: HACCP, ISO 22000, FSSC 22000, IFS\n- Аналози: чл. 49, ал. 2 ЗОП — допускат се при доказана еквивалентност\n- Типична гаранция за изпълнение: 4% от стойност без ДДС (3% за поръчки под левова равностойност на 70 000 EUR)\n- Авансово плащане до 20% срещу гаранция\n\nПринципи на отговора:\n- Винаги на български професионален език\n- Цитираш конкретни членове и алинеи на ЗОП/ППЗОП когато е приложимо\n- При неяснота молиш за уточнение; не познаваш\n- Кратко и структурирано — точки, не дълги абзаци\n- Когато получиш документация (PDF, Word, Excel), първо я класифицираш (вид процедура), после извличаш ключови параметри (предмет, количества, срокове, гаранции, критерии за оценка)'
where slot = 1;

update chats set
  title = 'Строителство',
  vertical = 'construction',
  icon = '🏗️',
  welcome_message = 'Готов съм за обществени поръчки за строителство. Качи документация или ме питай за изисквания по ЗУТ, КСС, категории строежи, гаранции за изпълнение и за добро изпълнение.',
  system_prompt = 'Ти си експерт-консултант по обществени поръчки за строителство в България. Работиш съгласно ЗОП, ППЗОП и ЗУТ.\n\nСпецифика на вертикала:\n- Категории строежи: I-V по ЗУТ — определят се по бруто строителна площ и сложност\n- КСС (Количествено-стойностна сметка) задължителна при строителство\n- FIDIC договори за по-големи проекти\n- Гаранция за участие: 1% от стойност без ДДС (max 5%)\n- Гаранция за изпълнение: 4% (3% за <70 000 EUR)\n- Гаранция за добро изпълнение в гаранционен период: 4% от стойност, освобождава се след изтичане\n- Авторски надзор по ЗУТ — отделна процедура или включен в основната\n- Изисквания за пожарна безопасност\n- Минимални изисквания за персонал: технически ръководител с минимум 5 г. опит\n- Аналози: чл. 49 ЗОП\n\nПринципи на отговора:\n- Винаги на български професионален език\n- Цитираш конкретни членове на ЗОП/ППЗОП/ЗУТ\n- При неяснота молиш за уточнение; не познаваш\n- Кратко и структурирано\n- При получаване на документация (ТС, КСС, инвестиционен проект), първо класифицираш, после извличаш категория строеж, прогнозна стойност, ключови позиции в КСС, срокове'
where slot = 2;

update chats set
  title = 'Строителен надзор',
  vertical = 'supervision',
  icon = '👷',
  welcome_message = 'Готов съм за поръчки за строителен надзор. Питай за изисквания за лиценз, експерти по специалности, застраховка професионална отговорност, период на отговорност 10 г.',
  system_prompt = 'Ти си експерт-консултант по обществени поръчки за строителен надзор в България. Работиш съгласно ЗОП, ППЗОП, ЗУТ и Наредба №3 за надзор.\n\nСпецифика на вертикала:\n- Лиценз за строителен надзор от ДНСК (Дирекция за национален строителен контрол)\n- Категории на обектите I-V определят изискваните експерти\n- Задължителни експерти по специалности: конструкции, ВиК, ел., ОВК, технологично, пожарна, геодезия, и др. — според категория\n- Минимален стаж на експертите: 3-5 г. в зависимост от категория\n- Застраховка професионална отговорност: задължителна, мин. 200 000 лв за категория I\n- Период на отговорност: 10 г. от въвеждане в експлоатация\n- Контролни функции: проектен надзор, контрол при изпълнение, протоколи и актове\n\nПринципи на отговора:\n- Винаги на български професионален език\n- Цитираш ЗУТ, Наредба №3, ЗОП където е приложимо\n- При неяснота молиш; не познаваш\n- При получаване на ТЗ за надзор — извличаш категория обект, изискван състав експерти, продължителност, прогнозна стойност'
where slot = 3;

update chats set
  title = 'Доставки',
  vertical = 'deliveries',
  icon = '📦',
  welcome_message = 'Готов съм за поръчки за доставки. Питай за гаранции за качество, срокове за гаранция, транспорт до краен адрес, CE и други сертификати, аналози.',
  system_prompt = 'Ти си експерт-консултант по обществени поръчки за доставки в България. Работиш съгласно ЗОП и ППЗОП.\n\nСпецифика на вертикала:\n- Разграничение между "доставка" (стока + транспорт) и "услуга" по чл. 3 ЗОП\n- Типична гаранция за изпълнение: 4% (3% за <70 000 EUR)\n- Гаранция за качество в гаранционен период: минимум 2 години\n- Транспорт и разтоварване до краен адрес обикновено за сметка на изпълнителя\n- CE маркировка за продукти от обхвата на регламентите\n- БДС стандарти според типа на продукта\n- Аналози по чл. 49 ЗОП — доказване на еквивалентност\n- Срокове за доставка: договорни, обикновено 30-90 дни\n- Авансово плащане до 20% срещу гаранция\n\nПринципи на отговора:\n- Винаги на български професионален език\n- Цитираш ЗОП/ППЗОП\n- При неяснота молиш; не познаваш\n- При получаване на ТЗ — извличаш предмет (продукт + количество), срок за доставка, гаранционен срок, изисквания за сертификати, методика за оценка'
where slot = 4;

update chats set
  title = 'Услуги',
  vertical = 'services',
  icon = '🛠️',
  welcome_message = 'Готов съм за поръчки за услуги. Питай за SLA изисквания, регулярни срещу еднократни услуги, експертен състав, сертификати ISO, опит на изпълнителя.',
  system_prompt = 'Ти си експерт-консултант по обществени поръчки за услуги в България (IT, консултантски, обучителни, поддръжка и др.). Работиш съгласно ЗОП и ППЗОП.\n\nСпецифика на вертикала:\n- Разграничение услуга / доставка по чл. 3 ЗОП\n- SLA (Service Level Agreement) — реакция, време за разрешаване, наличност\n- Регулярни услуги (с фиксиран периодичен план) vs еднократни\n- Изисквания за персонал: сертификати, опит, езикови познания\n- Сертификати ISO 9001, ISO/IEC 27001 за информационна сигурност, ISO 14001 за околна среда\n- Опит на изпълнителя — референции от подобни договори (стойност и предмет)\n- Гаранция за изпълнение: 4% (3% за <70 000 EUR)\n- Авансово до 20%\n\nПринципи на отговора:\n- Винаги на български професионален език\n- Цитираш ЗОП/ППЗОП\n- При неяснота молиш; не познаваш\n- При получаване на ТЗ — извличаш обхват на услугата, SLA, изисквания за персонал, продължителност, методика'
where slot = 5;

update chats set
  title = 'Рамкови споразумения',
  vertical = 'framework',
  icon = '📄',
  welcome_message = 'Готов съм за рамкови споразумения. Питай за разлика между "с един" и "с няколко" изпълнителя, минифонкурси, срок до 4 г., договорни количества.',
  system_prompt = 'Ти си експерт-консултант по рамкови споразумения в обществените поръчки в България. Работиш съгласно ЗОП и ППЗОП.\n\nСпецифика на вертикала:\n- Чл. 81-83 ЗОП — рамкови споразумения\n- Видове: рамково с един изпълнител vs рамково с няколко изпълнители\n- Срок: до 4 години; изключения до 7 години при добро обосноваване\n- Минифонкурси за конкретен предмет в рамките на рамковото — кратки срокове\n- Договорни количества: задължени и прогнозни\n- Възможност за централизирани и обединени възлагатели\n- Договори по рамкови: до изтичане срока на рамковото\n\nПринципи на отговора:\n- Винаги на български професионален език\n- Цитираш чл. 81-83 ЗОП и съответни членове на ППЗОП\n- При неяснота молиш; не познаваш\n- При получаване на ТЗ за рамково — изясняваш вид (един/няколко), срок, прогнозни количества, методика за избор на изпълнител в минифонкурс'
where slot = 6;
```

- [ ] **Step 2: Apply migration via Supabase MCP**

Use `mcp__supabase__apply_migration` with name `003_workspaces` and the full SQL above as `query`.

Expected: success, no error.

- [ ] **Step 3: Verify via SQL**

Use `mcp__supabase__execute_sql` with:

```sql
select slot, title, vertical, icon, length(system_prompt) as prompt_len from chats order by slot;
```

Expected: 6 rows, all with non-null vertical, icon (emoji), and prompt_len > 500.

- [ ] **Step 4: Commit migration file**

```bash
cd ZOPEXPERT
git add supabase/migrations/003_workspaces.sql
git commit -m "feat(sip2000): workspaces schema + seed 6 vertical specialisations"
```

---

## Task 2: System prompts constants (source of truth in code)

**Files:**
- Create: `ZOPEXPERT/lib/prompts/sip2000.ts`

The DB has the prompts seeded. We also store them in code so the repo is the source of truth and a future "sync to DB" command can keep them aligned.

- [ ] **Step 1: Create `ZOPEXPERT/lib/prompts/sip2000.ts`**

```typescript
export type Vertical =
  | "food"
  | "construction"
  | "supervision"
  | "deliveries"
  | "services"
  | "framework";

export type WorkspaceTemplate = {
  vertical: Vertical;
  slot: number;
  title: string;
  icon: string;
  welcomeMessage: string;
  systemPrompt: string;
};

export const SIP2000_WORKSPACES: Record<Vertical, WorkspaceTemplate> = {
  food: {
    vertical: "food",
    slot: 1,
    title: "Храни и хранителни продукти",
    icon: "🍎",
    welcomeMessage:
      "Готов съм да работя по обществени поръчки за храни. Качи документация или ме питай за изисквания по БДС, HACCP, хладилна верига, аналози по чл. 49 ЗОП.",
    systemPrompt: `Ти си експерт-консултант по обществени поръчки за храни и хранителни продукти в България. Работиш съгласно ЗОП и ППЗОП.

Специфика на вертикала:
- БДС стандарти за хранителни продукти (БДС EN 14108, БДС EN 13104 и др.)
- Хладилна верига и температурни режими (+2 до +8°C за охладени, под -18°C за дълбоко замразени)
- Срокове на годност в техническите спецификации
- Сертификати: HACCP, ISO 22000, FSSC 22000, IFS
- Аналози: чл. 49, ал. 2 ЗОП — допускат се при доказана еквивалентност
- Типична гаранция за изпълнение: 4% от стойност без ДДС (3% за поръчки под левова равностойност на 70 000 EUR)
- Авансово плащане до 20% срещу гаранция

Принципи на отговора:
- Винаги на български професионален език
- Цитираш конкретни членове и алинеи на ЗОП/ППЗОП когато е приложимо
- При неяснота молиш за уточнение; не познаваш
- Кратко и структурирано — точки, не дълги абзаци
- Когато получиш документация (PDF, Word, Excel), първо я класифицираш (вид процедура), после извличаш ключови параметри (предмет, количества, срокове, гаранции, критерии за оценка)`,
  },

  construction: {
    vertical: "construction",
    slot: 2,
    title: "Строителство",
    icon: "🏗️",
    welcomeMessage:
      "Готов съм за обществени поръчки за строителство. Качи документация или ме питай за изисквания по ЗУТ, КСС, категории строежи, гаранции за изпълнение и за добро изпълнение.",
    systemPrompt: `Ти си експерт-консултант по обществени поръчки за строителство в България. Работиш съгласно ЗОП, ППЗОП и ЗУТ.

Специфика на вертикала:
- Категории строежи: I-V по ЗУТ — определят се по бруто строителна площ и сложност
- КСС (Количествено-стойностна сметка) задължителна при строителство
- FIDIC договори за по-големи проекти
- Гаранция за участие: 1% от стойност без ДДС (max 5%)
- Гаранция за изпълнение: 4% (3% за <70 000 EUR)
- Гаранция за добро изпълнение в гаранционен период: 4% от стойност, освобождава се след изтичане
- Авторски надзор по ЗУТ — отделна процедура или включен в основната
- Изисквания за пожарна безопасност
- Минимални изисквания за персонал: технически ръководител с минимум 5 г. опит
- Аналози: чл. 49 ЗОП

Принципи на отговора:
- Винаги на български професионален език
- Цитираш конкретни членове на ЗОП/ППЗОП/ЗУТ
- При неяснота молиш за уточнение; не познаваш
- Кратко и структурирано
- При получаване на документация (ТС, КСС, инвестиционен проект), първо класифицираш, после извличаш категория строеж, прогнозна стойност, ключови позиции в КСС, срокове`,
  },

  supervision: {
    vertical: "supervision",
    slot: 3,
    title: "Строителен надзор",
    icon: "👷",
    welcomeMessage:
      "Готов съм за поръчки за строителен надзор. Питай за изисквания за лиценз, експерти по специалности, застраховка професионална отговорност, период на отговорност 10 г.",
    systemPrompt: `Ти си експерт-консултант по обществени поръчки за строителен надзор в България. Работиш съгласно ЗОП, ППЗОП, ЗУТ и Наредба №3 за надзор.

Специфика на вертикала:
- Лиценз за строителен надзор от ДНСК (Дирекция за национален строителен контрол)
- Категории на обектите I-V определят изискваните експерти
- Задължителни експерти по специалности: конструкции, ВиК, ел., ОВК, технологично, пожарна, геодезия, и др. — според категория
- Минимален стаж на експертите: 3-5 г. в зависимост от категория
- Застраховка професионална отговорност: задължителна, мин. 200 000 лв за категория I
- Период на отговорност: 10 г. от въвеждане в експлоатация
- Контролни функции: проектен надзор, контрол при изпълнение, протоколи и актове

Принципи на отговора:
- Винаги на български професионален език
- Цитираш ЗУТ, Наредба №3, ЗОП където е приложимо
- При неяснота молиш; не познаваш
- При получаване на ТЗ за надзор — извличаш категория обект, изискван състав експерти, продължителност, прогнозна стойност`,
  },

  deliveries: {
    vertical: "deliveries",
    slot: 4,
    title: "Доставки",
    icon: "📦",
    welcomeMessage:
      "Готов съм за поръчки за доставки. Питай за гаранции за качество, срокове за гаранция, транспорт до краен адрес, CE и други сертификати, аналози.",
    systemPrompt: `Ти си експерт-консултант по обществени поръчки за доставки в България. Работиш съгласно ЗОП и ППЗОП.

Специфика на вертикала:
- Разграничение между "доставка" (стока + транспорт) и "услуга" по чл. 3 ЗОП
- Типична гаранция за изпълнение: 4% (3% за <70 000 EUR)
- Гаранция за качество в гаранционен период: минимум 2 години
- Транспорт и разтоварване до краен адрес обикновено за сметка на изпълнителя
- CE маркировка за продукти от обхвата на регламентите
- БДС стандарти според типа на продукта
- Аналози по чл. 49 ЗОП — доказване на еквивалентност
- Срокове за доставка: договорни, обикновено 30-90 дни
- Авансово плащане до 20% срещу гаранция

Принципи на отговора:
- Винаги на български професионален език
- Цитираш ЗОП/ППЗОП
- При неяснота молиш; не познаваш
- При получаване на ТЗ — извличаш предмет (продукт + количество), срок за доставка, гаранционен срок, изисквания за сертификати, методика за оценка`,
  },

  services: {
    vertical: "services",
    slot: 5,
    title: "Услуги",
    icon: "🛠️",
    welcomeMessage:
      "Готов съм за поръчки за услуги. Питай за SLA изисквания, регулярни срещу еднократни услуги, експертен състав, сертификати ISO, опит на изпълнителя.",
    systemPrompt: `Ти си експерт-консултант по обществени поръчки за услуги в България (IT, консултантски, обучителни, поддръжка и др.). Работиш съгласно ЗОП и ППЗОП.

Специфика на вертикала:
- Разграничение услуга / доставка по чл. 3 ЗОП
- SLA (Service Level Agreement) — реакция, време за разрешаване, наличност
- Регулярни услуги (с фиксиран периодичен план) vs еднократни
- Изисквания за персонал: сертификати, опит, езикови познания
- Сертификати ISO 9001, ISO/IEC 27001 за информационна сигурност, ISO 14001 за околна среда
- Опит на изпълнителя — референции от подобни договори (стойност и предмет)
- Гаранция за изпълнение: 4% (3% за <70 000 EUR)
- Авансово до 20%

Принципи на отговора:
- Винаги на български професионален език
- Цитираш ЗОП/ППЗОП
- При неяснота молиш; не познаваш
- При получаване на ТЗ — извличаш обхват на услугата, SLA, изисквания за персонал, продължителност, методика`,
  },

  framework: {
    vertical: "framework",
    slot: 6,
    title: "Рамкови споразумения",
    icon: "📄",
    welcomeMessage:
      "Готов съм за рамкови споразумения. Питай за разлика между 'с един' и 'с няколко' изпълнителя, минифонкурси, срок до 4 г., договорни количества.",
    systemPrompt: `Ти си експерт-консултант по рамкови споразумения в обществените поръчки в България. Работиш съгласно ЗОП и ППЗОП.

Специфика на вертикала:
- Чл. 81-83 ЗОП — рамкови споразумения
- Видове: рамково с един изпълнител vs рамково с няколко изпълнители
- Срок: до 4 години; изключения до 7 години при добро обосноваване
- Минифонкурси за конкретен предмет в рамките на рамковото — кратки срокове
- Договорни количества: задължени и прогнозни
- Възможност за централизирани и обединени възлагатели
- Договори по рамкови: до изтичане срока на рамковото

Принципи на отговора:
- Винаги на български професионален език
- Цитираш чл. 81-83 ЗОП и съответни членове на ППЗОП
- При неяснота молиш; не познаваш
- При получаване на ТЗ за рамково — изясняваш вид (един/няколко), срок, прогнозни количества, методика за избор на изпълнител в минифонкурс`,
  },
};

export function getWorkspaceTemplate(vertical: string | null): WorkspaceTemplate | undefined {
  if (!vertical) return undefined;
  return SIP2000_WORKSPACES[vertical as Vertical];
}
```

- [ ] **Step 2: Commit**

```bash
cd ZOPEXPERT
git add lib/prompts/sip2000.ts
git commit -m "feat(sip2000): system prompts as TypeScript source of truth"
```

---

## Task 3: Extend Chat type

**Files:**
- Modify: `ZOPEXPERT/lib/types.ts`

- [ ] **Step 1: Replace `ZOPEXPERT/lib/types.ts` content with:**

```typescript
export type Chat = {
  id: string;
  slot: number;
  title: string;
  created_at: string;
  system_prompt: string | null;
  welcome_message: string | null;
  icon: string | null;
  vertical: string | null;
};

export type Attachment = {
  id: string;
  filename: string;
  mime: string;
  size: number;
  path: string;
  kind: "input" | "output";
};

export type Message = {
  id: string;
  chat_id: string;
  role: "user" | "assistant";
  content: string;
  created_at: string;
  attachments: Attachment[];
};
```

- [ ] **Step 2: Run typecheck**

```bash
cd ZOPEXPERT
export PATH="/c/Program Files/nodejs:$PATH"
npm run typecheck
```

Expected: clean (no errors).

- [ ] **Step 3: Commit**

```bash
cd ZOPEXPERT
git add lib/types.ts
git commit -m "feat(zopexpert): extend Chat type with workspace fields"
```

---

## Task 4: Hermes client wrapper with timeout + retry

**Files:**
- Create: `ZOPEXPERT/lib/hermes-client.ts`
- Create: `ZOPEXPERT/lib/hermes-client.test.ts`

This wraps the OpenAI streaming call with:
- **Timeout** — 50 seconds (Vercel hobby max is 60s; leaves 10s slack)
- **Empty-response retry** — if the entire stream produced no content, retry once
- **Grace message fallback** — if both attempts fail or time out, return a user-friendly Bulgarian message instead of a blank bubble

The wrapper returns a `ReadableStream<Uint8Array>` plus a promise that resolves to the collected full text (for downstream persistence).

- [ ] **Step 1: Write failing tests**

Create `ZOPEXPERT/lib/hermes-client.test.ts`:

```typescript
import { describe, it, expect, vi } from "vitest";

vi.mock("openai", () => ({
  default: class MockOpenAI {
    chat = {
      completions: {
        create: vi.fn(),
      },
    };
    constructor(_opts: unknown) {}
  },
}));

import OpenAI from "openai";
import { streamHermes } from "@/lib/hermes-client";

async function readAll(stream: ReadableStream<Uint8Array>): Promise<string> {
  const reader = stream.getReader();
  const decoder = new TextDecoder();
  let out = "";
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    out += decoder.decode(value, { stream: true });
  }
  return out;
}

function asyncGenOf(chunks: string[]) {
  return (async function* () {
    for (const c of chunks) {
      yield { choices: [{ delta: { content: c } }] };
    }
  })();
}

describe("streamHermes", () => {
  it("streams chunks from a successful Hermes call", async () => {
    const create = vi.fn().mockResolvedValue(asyncGenOf(["Здра", "вей", "!"]));
    (OpenAI as unknown as { prototype: { chat: { completions: { create: typeof create } } } }).prototype.chat = {
      completions: { create },
    };

    const { stream, fullText } = await streamHermes({
      messages: [{ role: "user", content: "hi" }],
      timeoutMs: 5000,
    });
    const text = await readAll(stream);
    expect(text).toBe("Здравей!");
    expect(await fullText).toBe("Здравей!");
  });

  it("retries once when first attempt returns empty stream", async () => {
    const create = vi
      .fn()
      .mockResolvedValueOnce(asyncGenOf([]))
      .mockResolvedValueOnce(asyncGenOf(["Втори опит"]));
    (OpenAI as unknown as { prototype: { chat: { completions: { create: typeof create } } } }).prototype.chat = {
      completions: { create },
    };

    const { stream } = await streamHermes({
      messages: [{ role: "user", content: "hi" }],
      timeoutMs: 5000,
    });
    const text = await readAll(stream);
    expect(text).toBe("Втори опит");
    expect(create).toHaveBeenCalledTimes(2);
  });

  it("returns grace message after both attempts produce empty content", async () => {
    const create = vi.fn().mockResolvedValue(asyncGenOf([]));
    (OpenAI as unknown as { prototype: { chat: { completions: { create: typeof create } } } }).prototype.chat = {
      completions: { create },
    };

    const { stream } = await streamHermes({
      messages: [{ role: "user", content: "hi" }],
      timeoutMs: 5000,
    });
    const text = await readAll(stream);
    expect(text).toContain("проблем");
    expect(create).toHaveBeenCalledTimes(2);
  });

  it("returns grace message when create throws on both attempts", async () => {
    const create = vi.fn().mockRejectedValue(new Error("hermes down"));
    (OpenAI as unknown as { prototype: { chat: { completions: { create: typeof create } } } }).prototype.chat = {
      completions: { create },
    };

    const { stream } = await streamHermes({
      messages: [{ role: "user", content: "hi" }],
      timeoutMs: 5000,
    });
    const text = await readAll(stream);
    expect(text).toContain("проблем");
    expect(create).toHaveBeenCalledTimes(2);
  });
});
```

- [ ] **Step 2: Run tests, confirm they FAIL**

```bash
cd ZOPEXPERT
export PATH="/c/Program Files/nodejs:$PATH"
npm test -- hermes-client
```

Expected: 4 tests fail with "Cannot find module".

- [ ] **Step 3: Create `ZOPEXPERT/lib/hermes-client.ts`:**

```typescript
import OpenAI from "openai";

const GRACE_MESSAGE =
  "Извинявам се, имах временен проблем с обработката. Моля, опитай отново или преформулирай въпроса.";
const MIN_VALID_CHARS = 1;
const MAX_RETRIES = 1; // 1 initial + 1 retry = 2 total attempts

export type HermesMessage = {
  role: "system" | "user" | "assistant";
  content: string;
};

export type StreamHermesOptions = {
  messages: HermesMessage[];
  timeoutMs?: number;
  model?: string;
};

export type StreamHermesResult = {
  stream: ReadableStream<Uint8Array>;
  fullText: Promise<string>;
};

function getClient(): OpenAI {
  return new OpenAI({
    baseURL: process.env.HERMES_BASE_URL,
    apiKey: process.env.HERMES_API_KEY!,
  });
}

async function collectAttempt(
  messages: HermesMessage[],
  model: string,
  timeoutMs: number,
): Promise<string | null> {
  const client = getClient();
  const controller = new AbortController();
  const timer = setTimeout(() => controller.abort(), timeoutMs);

  try {
    const result = await client.chat.completions.create(
      {
        model,
        stream: true,
        messages,
      },
      { signal: controller.signal },
    );

    let acc = "";
    for await (const chunk of result) {
      const piece = chunk.choices[0]?.delta?.content ?? "";
      if (piece) acc += piece;
    }
    return acc.length >= MIN_VALID_CHARS ? acc : null;
  } catch {
    return null;
  } finally {
    clearTimeout(timer);
  }
}

export async function streamHermes(
  options: StreamHermesOptions,
): Promise<StreamHermesResult> {
  const timeoutMs = options.timeoutMs ?? 50_000;
  const model = options.model ?? "hermes-agent";

  let resolveFullText!: (s: string) => void;
  const fullText = new Promise<string>((res) => {
    resolveFullText = res;
  });

  const encoder = new TextEncoder();
  const stream = new ReadableStream<Uint8Array>({
    async start(controller) {
      let text: string | null = null;
      for (let attempt = 0; attempt <= MAX_RETRIES; attempt++) {
        text = await collectAttempt(options.messages, model, timeoutMs);
        if (text) break;
      }

      const finalText = text ?? GRACE_MESSAGE;
      controller.enqueue(encoder.encode(finalText));
      controller.close();
      resolveFullText(finalText);
    },
  });

  return { stream, fullText };
}
```

- [ ] **Step 4: Run tests, confirm they PASS**

```bash
cd ZOPEXPERT
npm test -- hermes-client
```

Expected: `4 passed`.

- [ ] **Step 5: Run full suite to ensure nothing broke**

```bash
cd ZOPEXPERT
npm test
```

Expected: all tests pass (existing 17 + 4 new = 21).

- [ ] **Step 6: Commit**

```bash
cd ZOPEXPERT
git add lib/hermes-client.ts lib/hermes-client.test.ts
git commit -m "feat(zopexpert): add hermes-client with timeout + auto-retry + grace fallback"
```

---

## Task 5: Update `/api/hermes/chat` to use workspace prompt + hermes-client

**Files:**
- Modify: `ZOPEXPERT/app/api/hermes/chat/route.ts`
- Modify: `ZOPEXPERT/app/api/hermes/chat/route.test.ts`

The route fetches the workspace row (which now has `system_prompt`), prepends it as a system message, and uses `streamHermes` (instead of raw OpenAI client) so timeout + retry come for free. Streaming UX is preserved — the wrapper emits the full text at once when ready (acceptable trade-off vs. true token-by-token, given Hermes is fast).

- [ ] **Step 1: Replace `ZOPEXPERT/app/api/hermes/chat/route.test.ts` with:**

```typescript
import { describe, it, expect, vi } from "vitest";
import { NextRequest } from "next/server";

const insertCalls: Array<{
  chat_id: string;
  role: string;
  content: string;
  attachments?: unknown;
}> = [];

let mockChatRow: { system_prompt: string | null } = { system_prompt: null };

vi.mock("@/lib/supabase/server", () => ({
  createServiceClient: vi.fn(() => ({
    from: vi.fn((table: string) => {
      if (table === "chats") {
        return {
          select: vi.fn().mockReturnThis(),
          eq: vi.fn().mockReturnThis(),
          single: vi.fn().mockResolvedValue({ data: mockChatRow, error: null }),
        };
      }
      // messages
      return {
        insert: vi.fn((row: { chat_id: string; role: string; content: string; attachments?: unknown }) => {
          insertCalls.push(row);
          return Promise.resolve({ error: null });
        }),
        select: vi.fn().mockReturnThis(),
        eq: vi.fn().mockReturnThis(),
        order: vi.fn().mockReturnThis(),
        limit: vi.fn().mockResolvedValue({ data: [] }),
      };
    }),
    storage: {
      from: vi.fn(() => ({
        createSignedUrl: vi.fn().mockResolvedValue({
          data: { signedUrl: "https://test.supabase.co/storage/v1/object/sign/chat-files/abc.pdf?token=xyz" },
          error: null,
        }),
        list: vi.fn().mockResolvedValue({ data: [{ metadata: { size: 100, mimetype: "application/pdf" } }] }),
        download: vi.fn().mockResolvedValue({
          data: new Blob(["test file content"], { type: "text/plain" }),
          error: null,
        }),
      })),
    },
  })),
}));

const sentMessages: Array<Array<{ role: string; content: string }>> = [];

vi.mock("@/lib/hermes-client", () => ({
  streamHermes: vi.fn(async (opts: { messages: Array<{ role: string; content: string }> }) => {
    sentMessages.push(opts.messages);
    const text = "Hello world";
    const stream = new ReadableStream<Uint8Array>({
      start(controller) {
        controller.enqueue(new TextEncoder().encode(text));
        controller.close();
      },
    });
    return { stream, fullText: Promise.resolve(text) };
  }),
}));

import { POST } from "@/app/api/hermes/chat/route";

function makeRequest(body: unknown) {
  return new NextRequest("http://localhost/api/hermes/chat", {
    method: "POST",
    body: JSON.stringify(body),
    headers: { "Content-Type": "application/json" },
  });
}

describe("POST /api/hermes/chat", () => {
  beforeEach(() => {
    insertCalls.length = 0;
    sentMessages.length = 0;
    mockChatRow = { system_prompt: null };
  });

  it("returns 400 when chatId missing", async () => {
    const res = await POST(makeRequest({ message: "hi" }));
    expect(res.status).toBe(400);
  });

  it("returns 400 when message missing", async () => {
    const res = await POST(makeRequest({ chatId: "c1" }));
    expect(res.status).toBe(400);
  });

  it("streams text from Hermes when no attachments", async () => {
    const res = await POST(makeRequest({ chatId: "c1", message: "hi" }));
    expect(res.status).toBe(200);
    const text = await res.text();
    expect(text).toBe("Hello world");
  });

  it("prepends workspace system_prompt when present", async () => {
    mockChatRow = { system_prompt: "Ти си експерт по храни." };
    const res = await POST(makeRequest({ chatId: "c1", message: "hi" }));
    expect(res.status).toBe(200);
    await res.text();
    const sent = sentMessages[0];
    expect(sent[0]).toEqual({ role: "system", content: "Ти си експерт по храни." });
    expect(sent[sent.length - 1].role).toBe("user");
  });

  it("does not prepend system message when workspace has no prompt", async () => {
    mockChatRow = { system_prompt: null };
    const res = await POST(makeRequest({ chatId: "c1", message: "hi" }));
    expect(res.status).toBe(200);
    await res.text();
    const sent = sentMessages[0];
    expect(sent.some((m) => m.role === "system")).toBe(false);
  });

  it("accepts attachments and persists them on user message", async () => {
    const attachments = [
      {
        id: "a1",
        filename: "report.pdf",
        mime: "application/pdf",
        size: 100,
        path: "2026-05-21/abc-report.pdf",
        kind: "input",
      },
    ];
    const res = await POST(makeRequest({ chatId: "c1", message: "analyze", attachments }));
    expect(res.status).toBe(200);
    await res.text();
    const userInsert = insertCalls.find((c) => c.role === "user");
    expect(userInsert?.attachments).toEqual(attachments);
  });
});

import { beforeEach } from "vitest";
```

- [ ] **Step 2: Run tests, confirm new tests FAIL**

```bash
cd ZOPEXPERT
npm test -- hermes/chat
```

Expected: at least 2 tests fail (system prompt tests).

- [ ] **Step 3: Replace `ZOPEXPERT/app/api/hermes/chat/route.ts` with:**

```typescript
import { NextRequest, NextResponse } from "next/server";
import { createServiceClient } from "@/lib/supabase/server";
import { HISTORY_LIMIT, STORAGE_BUCKET } from "@/lib/files";
import { extractOutputUrls, urlsToAttachments } from "@/lib/output-urls";
import { extractText } from "@/lib/file-extract";
import { streamHermes, type HermesMessage } from "@/lib/hermes-client";
import type { Attachment } from "@/lib/types";

export const maxDuration = 60;

export async function POST(request: NextRequest) {
  const body = (await request.json()) as {
    chatId?: string;
    message?: string;
    attachments?: Attachment[];
  };

  if (!body.chatId) {
    return NextResponse.json({ error: "chatId is required" }, { status: 400 });
  }
  if (!body.message) {
    return NextResponse.json({ error: "message is required" }, { status: 400 });
  }

  const { chatId, message } = body;
  const attachments = body.attachments ?? [];
  const supabase = createServiceClient();

  // Fetch workspace system prompt
  const { data: chatRow } = await supabase
    .from("chats")
    .select("system_prompt")
    .eq("id", chatId)
    .single();
  const systemPrompt = (chatRow as { system_prompt: string | null } | null)?.system_prompt ?? null;

  // Persist user message with attachments
  await supabase.from("messages").insert({
    chat_id: chatId,
    role: "user",
    content: message,
    attachments,
  });

  // Server-side: download each attachment, extract text, inline in user message
  let attachmentBlock = "";
  if (attachments.length > 0) {
    const sections: string[] = [];
    for (const a of attachments) {
      const { data, error } = await supabase.storage
        .from(STORAGE_BUCKET)
        .download(a.path);
      if (error || !data) {
        sections.push(
          `=== Файл: ${a.filename} (${a.mime}, ${a.size} bytes) ===\n[Файлът не можа да бъде свален от хранилището: ${error?.message ?? "неизвестна грешка"}]\n=== Край на файла ===`,
        );
        continue;
      }
      const buffer = Buffer.from(await data.arrayBuffer());
      const extracted = await extractText(buffer, a.mime, a.filename);
      if (extracted.ok && extracted.text !== undefined) {
        const truncatedNote = extracted.truncated
          ? `\n\n[... съкратено, общо ${extracted.originalLength} символа в оригиналния файл]`
          : "";
        sections.push(
          `=== Файл: ${a.filename} (${a.mime}, ${a.size} bytes) ===\n${extracted.text}${truncatedNote}\n=== Край на файла ===`,
        );
      } else {
        sections.push(
          `=== Файл: ${a.filename} (${a.mime}, ${a.size} bytes) ===\n[${extracted.reason ?? "Съдържанието не можа да бъде извлечено"}]\n=== Край на файла ===`,
        );
      }
    }
    attachmentBlock = `Потребителят прикачи файлове. По-долу е извлеченото съдържание. Отговори на база на това съдържание.\n\n${sections.join(
      "\n\n",
    )}\n\n---\n\nСъобщение на потребителя: ${message}`;
  }

  const outboundContent = attachmentBlock || message;

  // Load up to HISTORY_LIMIT messages for context
  const { data: history } = await supabase
    .from("messages")
    .select("role, content")
    .eq("chat_id", chatId)
    .order("created_at", { ascending: true })
    .limit(HISTORY_LIMIT);

  const contextMessages = ((history ?? []) as Array<{ role: "user" | "assistant"; content: string }>).slice(
    0,
    -1, // drop the just-inserted user row; replace with augmented version
  );
  contextMessages.push({ role: "user", content: outboundContent });

  // Compose messages: optional system + history
  const messages: HermesMessage[] = [];
  if (systemPrompt && systemPrompt.trim().length > 0) {
    messages.push({ role: "system", content: systemPrompt });
  }
  for (const m of contextMessages) {
    messages.push({ role: m.role, content: m.content });
  }

  const { stream, fullText } = await streamHermes({ messages });

  // Persist assistant message after stream completes (in parallel with delivery)
  fullText.then(async (text) => {
    const outputUrls = extractOutputUrls(text);
    const outputAttachments = await urlsToAttachments(outputUrls);
    await supabase.from("messages").insert({
      chat_id: chatId,
      role: "assistant",
      content: text,
      attachments: outputAttachments,
    });
  });

  return new Response(stream, {
    headers: {
      "Content-Type": "text/plain; charset=utf-8",
      "Transfer-Encoding": "chunked",
    },
  });
}
```

- [ ] **Step 4: Run tests, confirm all PASS**

```bash
cd ZOPEXPERT
npm test
```

Expected: all 21+ tests pass.

- [ ] **Step 5: Run typecheck**

```bash
cd ZOPEXPERT
npm run typecheck
```

Expected: clean.

- [ ] **Step 6: Commit**

```bash
cd ZOPEXPERT
git add app/api/hermes/chat/
git commit -m "feat(sip2000): chat route uses workspace system prompt + hermes-client wrapper"
```

---

## Task 6: Update `ChatPanel` to show icon, welcome message, custom placeholder

**Files:**
- Modify: `ZOPEXPERT/components/ChatPanel.tsx`

Changes:
- Header shows `chat.icon` to the left of `chat.title`
- Empty state uses `chat.welcome_message` (falls back to current default)
- Placeholder mentions the vertical when known

- [ ] **Step 1: Read current `ZOPEXPERT/components/ChatPanel.tsx`**

(Inspector subagents already know the file. This step just notes it must be read before edited.)

- [ ] **Step 2: Replace the header span and empty-state block**

Find this in `ChatPanel.tsx`:

```typescript
        <span
          style={{
            fontWeight: 600,
            fontSize: 13,
            color: "var(--color-text-primary)",
          }}
        >
          {chat.title}
        </span>
```

Replace with:

```typescript
        <span
          style={{
            fontWeight: 600,
            fontSize: 13,
            color: "var(--color-text-primary)",
            display: "flex",
            alignItems: "center",
            gap: 6,
          }}
        >
          {chat.icon && <span style={{ fontSize: 14 }}>{chat.icon}</span>}
          {chat.title}
        </span>
```

Then find this in the same file:

```typescript
          {messages.length === 0 && !isLoading && (
            <p
              style={{
                color: "var(--color-text-tertiary)",
                fontSize: 12,
                textAlign: "center",
                marginTop: 24,
              }}
            >
              Напишете нещо или плъзнете файл тук...
            </p>
          )}
```

Replace with:

```typescript
          {messages.length === 0 && !isLoading && (
            <p
              style={{
                color: "var(--color-text-tertiary)",
                fontSize: 12,
                textAlign: "center",
                marginTop: 24,
                padding: "0 24px",
                lineHeight: 1.6,
              }}
            >
              {chat.welcome_message ?? "Напишете нещо или плъзнете файл тук..."}
            </p>
          )}
```

Then find the textarea placeholder:

```typescript
          placeholder="Съобщение... (Enter за изпращане)"
```

Replace with:

```typescript
          placeholder={
            chat.vertical
              ? `Питай за ${chat.title.toLowerCase()}... (Enter за изпращане)`
              : "Съобщение... (Enter за изпращане)"
          }
```

- [ ] **Step 3: Run typecheck**

```bash
cd ZOPEXPERT
npm run typecheck
```

Expected: clean.

- [ ] **Step 4: Run build**

```bash
cd ZOPEXPERT
npm run build 2>&1 | tail -15
```

Expected: `✓ Compiled successfully`.

- [ ] **Step 5: Commit**

```bash
cd ZOPEXPERT
git add components/ChatPanel.tsx
git commit -m "feat(sip2000): ChatPanel renders icon + welcome message + vertical placeholder"
```

---

## Task 7: Deploy + smoke test

**Files:** none (deployment only)

- [ ] **Step 1: Push to GitHub**

```bash
cd "C:\Users\User\Documents\Бизнес\ZOPEXPERT"
git push "https://emmgivailopetev38-png:<GITHUB_PAT>@github.com/emmgivailopetev38-png/Petyr-Petev.git" main:main
```

- [ ] **Step 2: Deploy to Vercel production**

```bash
cd "C:\Users\User\Documents\Бизнес\ZOPEXPERT"
export PATH="/c/Users/User/AppData/Roaming/npm:/c/Program Files/nodejs:$PATH"
vercel --prod 2>&1 | tail -8
```

Expected: deployment READY, URL contains `vercel.app`.

- [ ] **Step 3: Smoke test — login + load + send a vertical-specific question**

```bash
rm -f /tmp/zop.txt
curl -s -X POST "https://zopexpert.vercel.app/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"password":"ZopExpert2026!Secure"}' \
  -c /tmp/zop.txt -o /dev/null

# Get Workspace 2 (Construction) id
SK="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImdncWF5cGtkb3ZxdXVxaXNnbGlwIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3OTM1OTQzMiwiZXhwIjoyMDk0OTM1NDMyfQ.R8oiH1sDm1vxYQCV1tCnGSQuTUqofkGDx3pmlfGKpTA"
CONSTRUCTION_ID=$(curl -s "https://ggqaypkdovquuqisglip.supabase.co/rest/v1/chats?select=id&slot=eq.2" -H "apikey: $SK" | grep -oE '"id":"[^"]+"' | cut -d'"' -f4)
echo "Construction workspace: $CONSTRUCTION_ID"

# Ask a construction-specific question
curl -s -X POST "https://zopexpert.vercel.app/api/hermes/chat" \
  -H "Content-Type: application/json" -b /tmp/zop.txt \
  -d "{\"chatId\":\"$CONSTRUCTION_ID\",\"message\":\"Каква е гаранцията за изпълнение за процедура под левова равностойност на 70 000 EUR?\"}" \
  --max-time 60 -w "\nStatus: %{http_code}\n"
```

Expected output contains `3%` (the vertical-specific answer Hermes should now know to give without being told it's a construction expert).

- [ ] **Step 4: Visual check in browser**

Open https://zopexpert.vercel.app , log in, observe:

- Each of the 6 panels shows a distinct icon (🍎 🏗️ 👷 📦 🛠️ 📄)
- Each panel's empty state shows its own welcome message in Bulgarian
- Each panel's textarea placeholder mentions the vertical title
- Sending a vertical-specific question gives a relevant answer

- [ ] **Step 5: Final commit marker**

```bash
cd "C:\Users\User\Documents\Бизнес\ZOPEXPERT"
git commit --allow-empty -m "release(sip2000): phase 1 — specialised workspaces live"
git push "https://emmgivailopetev38-png:<GITHUB_PAT>@github.com/emmgivailopetev38-png/Petyr-Petev.git" main:main
```

---

## Post-Phase notes

- **Editable prompts:** Phase 1 ships read-only prompts seeded in DB. A Phase 1.5 admin UI for editing per-workspace `system_prompt`/`welcome_message` is a 1-day add when needed.
- **Self-critique loop:** Deferred. Easy to bolt on inside `streamHermes` (second pass with a critique-focused prompt). 1-day add.
- **RAG / knowledge_chunks:** Phase 3. The current contract (workspace has `system_prompt`) composes cleanly with future RAG (retrieved chunks get appended to the system message before send).
- **Playbooks:** Phase 2. New `playbooks` + `playbook_runs` tables, server-side state machine, each step calls `streamHermes` with a focused step prompt — same wrapper.
